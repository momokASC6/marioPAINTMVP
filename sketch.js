function setup() {
    createCanvas(500,500); // Set canvas to 500x500
}

function mouseDragged() {
    // Create a circle with origin of the mouse's X and Y coordinates
    //circle(mouseX, mouseY, 100);
}